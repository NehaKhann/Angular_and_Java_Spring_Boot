import { ANCommerceValidators } from './ancommerce-validators';

describe('ANCommerceValidators', () => {
  it('should create an instance', () => {
    expect(new ANCommerceValidators()).toBeTruthy();
  });
});
